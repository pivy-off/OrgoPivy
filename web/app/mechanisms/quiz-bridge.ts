/**
 * Future hook: mechanism steps ↔ quiz items (reaction prediction, arrow pushing).
 * Import from quiz modules without coupling to SVG step data yet.
 */
export const MECHANISM_QUIZ_BRIDGE_VERSION = 1;
